/**
 * PayPal Server SDKLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */
import { Schema } from '../schema.js';
import { LinkDescription } from './linkDescription.js';
import { MerchantPreferences } from './merchantPreferences.js';
import { PaymentPreferences } from './paymentPreferences.js';
import { SubscriptionBillingCycle } from './subscriptionBillingCycle.js';
import { SubscriptionPlanStatus } from './subscriptionPlanStatus.js';
import { Taxes } from './taxes.js';
/** The plan details. */
export interface BillingPlan {
    /** The unique PayPal-generated ID for the plan. */
    id?: string;
    /** The ID for the product. */
    productId?: string;
    /** The plan name. */
    name?: string;
    /** The plan status. */
    status?: SubscriptionPlanStatus;
    /** The detailed description of the plan. */
    description?: string;
    /** An array of billing cycles for trial billing and regular billing. A plan can have at most two trial cycles and only one regular cycle. */
    billingCycles?: SubscriptionBillingCycle[];
    /** The payment preferences for a subscription. */
    paymentPreferences?: PaymentPreferences;
    /** The merchant preferences for a subscription. */
    merchantPreferences?: MerchantPreferences;
    /** The tax details. */
    taxes?: Taxes;
    /** Indicates whether you can subscribe to this plan by providing a quantity for the goods or service. */
    quantitySupported?: boolean;
    /** The date and time, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Seconds are required while fractional seconds are optional. Note: The regular expression provides guidance but does not reject all invalid dates. */
    createTime?: string;
    /** The date and time, in [Internet date and time format](https://tools.ietf.org/html/rfc3339#section-5.6). Seconds are required while fractional seconds are optional. Note: The regular expression provides guidance but does not reject all invalid dates. */
    updateTime?: string;
    /** An array of request-related [HATEOAS links](/docs/api/reference/api-responses/#hateoas-links). */
    links?: LinkDescription[];
}
export declare const billingPlanSchema: Schema<BillingPlan>;
//# sourceMappingURL=billingPlan.d.ts.map